[project]
name = "agi-repository"
version = "0.1.0"
description = "A compact live interview exercise around a mocked GenAI RAG pipeline."
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2",
    "scikit-learn>=1.5",
]

[tool.uv]
package = false
